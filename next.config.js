/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com","picsum.photos","react-shooping-cart.netlify.app"]
  }
}

module.exports = nextConfig
